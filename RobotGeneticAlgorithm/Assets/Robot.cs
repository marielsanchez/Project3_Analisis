using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Robot : MonoBehaviour
{
    private bool genes = [];
    private int fitness = 0;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }
    
    /***
    Robot Start()
    {
        retun Robot;
    }
    
    GenerateRobot()// fills up the ADN chain
    CrossOver recibe un Robot, lo hace con otro robot y luego
    recuelve un nuevo child
    
     */

    // Update is called once per frame
    void Update()
    {
        
    }
}
